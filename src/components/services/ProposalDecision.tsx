"use client";import {useState} from "react";export function ProposalDecision({token,status}:{token:string;status:string}){const[name,setName]=useState(""),[message,setMessage]=useState("");async function decide(action:string){const r=await fetch(`/api/services/proposals/${token}`,{method:"PATCH",headers:{"Content-Type":"application/json"},body:JSON.stringify({name,action})});const d=await r.json();setMessage(r.ok?action==="accepted"?"Proposta aceita! Agora você pode pagar o sinal para reservar a data.":"Decisão registrada. A M&M entrará em contato.":d.error)}if(status!=="sent"&&status!=="viewed")return <p className="rounded-xl bg-neutral-900 p-5 font-bold">Status desta proposta: {status}</p>;return <div className="rounded-2xl border border-neutral-800 bg-neutral-950 p-6"><label className="grid gap-2"><span>Nome completo de quem está aceitando</span><input value={name} onChange={e=>setName(e.target.value)} className="rounded-xl border border-neutral-700 bg-black p-4"/></label><p className="mt-3 text-xs text-neutral-500">Ao aceitar, ficam registrados nome, data, horário e IP como evidência eletrônica do aceite.</p><div className="mt-5 flex flex-wrap gap-3"><button onClick={()=>decide("accepted")} className="rounded-xl bg-green-700 px-6 py-4 font-black">Aceitar proposta</button><button onClick={()=>decide("declined")} className="rounded-xl bg-neutral-800 px-6 py-4 font-bold">Recusar</button></div>{message&&<p className="mt-4 rounded-lg bg-black p-3">{message}</p>}</div>}
