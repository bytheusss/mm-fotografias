"use client";import {useState} from "react";export function DepositButton({token}:{token:string}){const[loading,setLoading]=useState(false),[error,setError]=useState("");async function pay(){setLoading(true);setError("");const r=await fetch(`/api/services/proposals/${token}/payment`,{method:"POST"}),d=await r.json();if(r.ok)location.href=d.checkoutUrl;else{setError(d.error||"Não foi possível abrir o pagamento.");setLoading(false)}}return <div className="rounded-2xl border border-green-900 bg-green-950/20 p-6"><h2 className="text-2xl font-black">Reserve sua data</h2><p className="mt-2 text-neutral-300">Pague o sinal com PIX ou cartão pelo Mercado Pago.</p><button disabled={loading} onClick={pay} className="mt-5 rounded-xl bg-green-700 px-6 py-4 font-black disabled:opacity-50">{loading?"Abrindo pagamento…":"Pagar sinal"}</button>{error&&<p className="mt-3 text-red-400">{error}</p>}</div>}
