"use client";import{useState}from"react";export function ContractDecision({token,disabled=false}:{token:string;disabled?:boolean}){const[name,setName]=useState(""),[document,setDocument]=useState(""),[message,setMessage]=useState(""),[busy,setBusy]=useState(false);async function decide(action:string){setBusy(true);const r=await fetch(`/api/contracts/${token}`,{method:"PATCH",headers:{"Content-Type":"application/json"},body:JSON.stringify({name,document,action})}),d=await r.json();setMessage(r.ok?(action==="accepted"?"Contrato aceito e registrado.":"Recusa registrada."):d.error||"Não foi possível registrar.");setBusy(false);if(r.ok)setTimeout(()=>location.reload(),800)}if(disabled)return null;return <section className="no-print mt-8 rounded-2xl border border-red-900 bg-neutral-950 p-6"><h2 className="text-2xl font-black">Aceite eletrônico</h2><p className="mt-2 text-sm text-neutral-400">Leia integralmente antes de decidir. O sistema registrará data, IP e versão deste documento.</p><div className="mt-5 grid gap-3 sm:grid-cols-2"><input value={name} onChange={e=>setName(e.target.value)} placeholder="Nome completo" className="rounded-lg border border-neutral-700 bg-black p-4"/><input value={document} onChange={e=>setDocument(e.target.value)} placeholder="CPF ou documento" className="rounded-lg border border-neutral-700 bg-black p-4"/></div><div className="mt-4 grid gap-3 sm:grid-cols-2"><button disabled={busy} onClick={()=>decide("accepted")} className="rounded-lg bg-green-700 p-4 font-black">Aceitar contrato</button><button disabled={busy} onClick={()=>decide("declined")} className="rounded-lg border border-red-800 p-4 font-black text-red-400">Recusar</button></div>{message&&<p className="mt-3 rounded-lg bg-black p-3">{message}</p>}</section>}
