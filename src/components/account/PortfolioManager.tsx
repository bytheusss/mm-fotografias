"use client";
import Image from "next/image";import{useEffect,useState}from"react";
type Asset={id:string;url:string;title:string;description:string;category:string;published:boolean;sort_order:number};
const categories=["Casamentos","Automotivo","Retratos","Gestante","Família","Aniversários","Corporativo","Eventos","Outros"];
export function PortfolioManager(){const[assets,setAssets]=useState<Asset[]>([]),[files,setFiles]=useState<File[]>([]),[category,setCategory]=useState(categories[0]),[busy,setBusy]=useState(false),[message,setMessage]=useState("");const load=()=>fetch("/api/profile/portfolio").then(r=>r.json()).then(data=>setAssets(data.assets||[]));useEffect(()=>{void load()},[]);async function upload(){if(!files.length)return;setBusy(true);setMessage("Otimizando e enviando suas fotos…");const form=new FormData();files.forEach(file=>form.append("files",file));form.set("category",category);const response=await fetch("/api/profile/portfolio",{method:"POST",body:form}),data=await response.json();setMessage(response.ok?`${data.assets.length} foto(s) adicionada(s).`:data.error||"Não foi possível enviar.");if(response.ok){setFiles([]);await load()}setBusy(false)}async function save(asset:Asset){const response=await fetch("/api/profile/portfolio",{method:"PATCH",headers:{"Content-Type":"application/json"},body:JSON.stringify({...asset,sortOrder:asset.sort_order})});setMessage(response.ok?"Alterações salvas.":"Não foi possível salvar.")}async function remove(asset:Asset){if(!confirm("Excluir esta foto do portfólio?"))return;const response=await fetch("/api/profile/portfolio",{method:"DELETE",headers:{"Content-Type":"application/json"},body:JSON.stringify({id:asset.id})});if(response.ok)setAssets(current=>current.filter(item=>item.id!==asset.id));else setMessage("Não foi possível excluir.")}function update(id:string,changes:Partial<Asset>){setAssets(current=>current.map(item=>item.id===id?{...item,...changes}:item))}return <div className="space-y-8"><section className="rounded-2xl border border-neutral-800 bg-neutral-900 p-6"><h2 className="text-2xl font-black">Adicionar trabalhos autorais</h2><p className="mt-2 text-neutral-400">Estas fotos não precisam pertencer a um evento. Elas aparecem no seu perfil e no Portfólio da M&M.</p><div className="mt-5 grid gap-4 md:grid-cols-[1fr_240px]"><input type="file" multiple accept="image/*" disabled={busy} onChange={e=>setFiles(Array.from(e.target.files||[]))} className="rounded-lg bg-black p-3"/><select value={category} onChange={e=>setCategory(e.target.value)} className="rounded-lg bg-black p-3">{categories.map(item=><option key={item}>{item}</option>)}</select></div><button onClick={upload} disabled={busy||!files.length} className="mt-4 w-full rounded-xl bg-red-600 p-4 font-black disabled:opacity-50">{busy?"Enviando…":`Enviar ${files.length||""} foto(s)`}</button>{message&&<p className="mt-3 rounded-lg bg-black p-3 text-sm" aria-live="polite">{message}</p>}</section><section><h2 className="mb-5 text-2xl font-black">Meu portfólio</h2><div className="grid gap-5 md:grid-cols-2">{assets.map(asset=><article key={asset.id} className="overflow-hidden rounded-2xl border border-neutral-800 bg-neutral-900"><div className="relative aspect-[4/3]"><Image src={asset.url} alt={asset.title||"Foto do portfólio"} fill className="object-cover" sizes="(max-width:768px) 100vw, 50vw"/></div><div className="space-y-3 p-4"><input value={asset.title||""} onChange={e=>update(asset.id,{title:e.target.value})} placeholder="Título da foto" className="w-full rounded bg-black p-3"/><textarea value={asset.description||""} onChange={e=>update(asset.id,{description:e.target.value})} placeholder="Descrição (opcional)" className="w-full rounded bg-black p-3"/><div className="grid grid-cols-[1fr_90px] gap-3"><select value={asset.category} onChange={e=>update(asset.id,{category:e.target.value})} className="rounded bg-black p-3">{categories.map(item=><option key={item}>{item}</option>)}</select><input type="number" value={asset.sort_order} onChange={e=>update(asset.id,{sort_order:Number(e.target.value)})} aria-label="Ordem" className="rounded bg-black p-3"/></div><label className="flex items-center gap-2"><input type="checkbox" checked={asset.published} onChange={e=>update(asset.id,{published:e.target.checked})}/> Publicar no site</label><div className="grid grid-cols-2 gap-3"><button onClick={()=>save(asset)} className="rounded-lg bg-red-600 p-3 font-bold">Salvar</button><button onClick={()=>remove(asset)} className="rounded-lg border border-neutral-700 p-3">Excluir</button></div></div></article>)}</div>{!assets.length&&<p className="rounded-xl border border-neutral-800 p-6 text-neutral-400">Você ainda não adicionou trabalhos independentes.</p>}</section></div>}
