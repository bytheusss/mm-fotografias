"use client";import {useEffect,useState} from "react";type Device={id:string;device_key:string;label?:string;user_agent?:string;last_seen_at:string};
export function DeviceManager(){const[open,setOpen]=useState(false),[rows,setRows]=useState<Device[]>([]),[message,setMessage]=useState("");async function load(){const r=await fetch("/api/account/devices",{cache:"no-store"}),d=await r.json();if(r.ok)setRows(d.devices||[])}useEffect(()=>{if(open)void load()},[open]);async function remove(id:string){const r=await fetch("/api/account/devices",{method:"DELETE",headers:{"Content-Type":"application/json"},body:JSON.stringify({id})});setMessage(r.ok?"Dispositivo removido.":"Não foi possível remover.");if(r.ok)await load()}return <section className="my-6 rounded-xl border border-neutral-800 bg-neutral-900 p-5"><button type="button" onClick={()=>setOpen(value=>!value)} className="flex w-full items-center justify-between text-left"><span><b className="text-xl">Dispositivos da conta</b><small className="mt-1 block text-neutral-400">Confira onde sua biblioteca está sincronizada.</small></span><span>{open?"−":"+"}</span></button>{open&&<div className="mt-4 space-y-2">{rows.map(row=><article key={row.id} className="flex flex-wrap items-center justify-between gap-3 rounded-lg bg-black p-3"><div className="min-w-0"><b>{row.label||"Dispositivo"}</b><p className="truncate text-xs text-neutral-500">{row.user_agent}</p><p className="text-xs text-neutral-400">Visto em {new Date(row.last_seen_at).toLocaleString("pt-BR")}</p></div><button onClick={()=>remove(row.id)} className="rounded border border-red-900 px-3 py-2 text-sm text-red-400">Remover</button></article>)}{!rows.length&&<p className="text-neutral-400">Nenhum dispositivo registrado.</p>}{message&&<p className="text-sm" aria-live="polite">{message}</p>}</div>}</section>}
