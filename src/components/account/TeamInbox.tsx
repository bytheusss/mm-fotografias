"use client";import Link from "next/link";type Notice={id:string;title:string;body?:string;href?:string};type Request={id:string;amount:number;status:string};export function TeamInbox({notifications,requests}:{notifications:Notice[];requests:Request[]}){return <section className="my-6 grid gap-4 md:grid-cols-2"><div className="rounded-xl border border-neutral-800 bg-neutral-900 p-5"><h2 className="text-xl font-bold">Notificações</h2><div className="mt-3 space-y-2">{notifications.map(n=><article key={n.id} className="rounded bg-black p-3"><b>{n.title}</b>{n.body&&<p className="text-sm text-neutral-400">{n.body}</p>}{n.href&&<Link href={n.href} className="text-sm text-red-400">Abrir</Link>}</article>)}{!notifications.length&&<p className="text-neutral-400">Nenhuma notificação nova.</p>}</div></div><div className="rounded-xl border border-neutral-800 bg-neutral-900 p-5"><h2 className="text-xl font-bold">Solicitações de repasse</h2><div className="mt-3 space-y-2">{requests.map(r=><p key={r.id} className="rounded bg-black p-3">R$ {Number(r.amount).toFixed(2).replace(".",",")} · {r.status}</p>)}{!requests.length&&<p className="text-neutral-400">Nenhuma solicitação enviada.</p>}</div></div></section>}
