"use client";
import { useState } from "react";
export function EventPasswordGate({ slug, name }: { slug: string; name: string }) { const [password,setPassword]=useState(""); const [error,setError]=useState(""); const [loading,setLoading]=useState(false); return <main className="grid min-h-screen place-items-center bg-black px-6 pt-24 text-white"><form onSubmit={async e=>{e.preventDefault();setLoading(true);setError("");const response=await fetch(`/api/events/${slug}/unlock`,{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({password})});const data=await response.json();if(response.ok)location.reload();else{setError(data.error||"Acesso negado.");setLoading(false);}}} className="w-full max-w-md rounded-xl border border-neutral-800 bg-neutral-900 p-7"><h1 className="text-3xl font-bold">Álbum protegido</h1><p className="mt-2 text-neutral-400">Digite a senha para acessar {name}.</p><input autoFocus required type="password" value={password} onChange={e=>setPassword(e.target.value)} className="mt-6 w-full rounded-lg border border-neutral-700 bg-black p-3" placeholder="Senha do evento" />{error&&<p className="mt-3 text-red-400">{error}</p>}<button disabled={loading} className="mt-5 w-full rounded-lg bg-red-600 p-3 font-bold">{loading?"Verificando…":"Acessar álbum"}</button></form></main>; }
