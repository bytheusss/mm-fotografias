"use client";import {useState} from "react";import {ChatPanel} from "@/components/chat/ChatPanel";const topics=["Encontrar minhas fotos","Pagamento ou pedido","Download das fotos","Privacidade e remoção","Falar com um fotógrafo","Orçamento para evento"];export function HelpDesk({logged}:{logged:boolean}){const[topic,setTopic]=useState(""),[detail,setDetail]=useState("");const message=`Olá! Preciso de ajuda com: ${topic||"atendimento"}.${detail?`\n\nDetalhes: ${detail}`:""}\n\nVim pela Central de Ajuda do site.`;return <div className="grid gap-8 lg:grid-cols-2"><section className="rounded-2xl border border-neutral-800 bg-neutral-900 p-6"><p className="font-bold text-red-400">Assistente M&M</p><h2 className="mt-2 text-2xl font-black">Como podemos ajudar?</h2><p className="mt-2 text-neutral-400">Escolha um assunto. O assistente organiza sua solicitação e abre o WhatsApp já preenchido.</p><div className="mt-5 grid gap-2 sm:grid-cols-2">{topics.map(item=><button key={item} onClick={()=>setTopic(item)} className={`rounded-xl border p-3 text-left ${topic===item?"border-red-500 bg-red-950/30":"border-neutral-700"}`}>{item}</button>)}</div><textarea value={detail} onChange={e=>setDetail(e.target.value)} rows={4} className="mt-4 w-full rounded-xl bg-black p-4" placeholder="Conte mais (opcional)"/><a href={`https://wa.me/5519999516802?text=${encodeURIComponent(message)}`} target="_blank" rel="noreferrer" className="mt-4 block rounded-xl bg-green-700 p-4 text-center font-black">Abrir atendimento no WhatsApp</a></section><section><h2 className="mb-3 text-2xl font-black">Chat com a equipe</h2>{logged?<ChatPanel/>:<div className="rounded-2xl border border-neutral-800 bg-neutral-900 p-6"><p className="text-neutral-300">Entre na sua conta para conversar com a equipe e manter o histórico.</p><a href="/login?next=/ajuda" className="mt-4 inline-block rounded-lg bg-red-600 px-5 py-3 font-bold">Entrar para conversar</a></div>}</section></div>}
