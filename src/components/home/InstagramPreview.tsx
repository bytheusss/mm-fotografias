import Image from "next/image";
import { Container } from "@/components/ui/Container";
import { getFeaturedPhotos } from "@/lib/events";
import { SITE } from "@/lib/constants/site";
export async function InstagramPreview() { const photos = await getFeaturedPhotos(6); if (!photos.length) return null; return <section className="py-16"><Container><div className="mb-7 flex flex-wrap items-end justify-between gap-4"><div><p className="text-sm font-bold uppercase tracking-[.2em] text-red-500">Acompanhe nosso trabalho</p><h2 className="mt-2 text-3xl font-black md:text-4xl">M&M no Instagram</h2><p className="mt-2 text-neutral-400">Uma prévia dos registros que você encontra por lá.</p></div><a href={SITE.instagram} target="_blank" rel="noopener noreferrer" className="rounded-lg bg-gradient-to-r from-purple-600 via-pink-600 to-orange-500 px-5 py-3 font-bold text-white">Ver {SITE.instagramHandle}</a></div><div className="grid grid-cols-3 gap-1.5 md:gap-3">{photos.map(photo => <a key={photo.id} href={SITE.instagram} target="_blank" rel="noopener noreferrer" className="group relative aspect-square overflow-hidden rounded md:rounded-lg" aria-label={`Ver Instagram — foto ${photo.numero}`}><Image src={photo.thumbnail} alt={`Trabalho M&M Fotografias — foto ${photo.numero}`} fill sizes="(max-width:768px) 33vw, 300px" className="object-cover transition duration-500 group-hover:scale-105 group-hover:brightness-75"/><span className="absolute inset-0 grid place-items-center bg-black/0 text-2xl opacity-0 transition group-hover:bg-black/30 group-hover:opacity-100">◎</span></a>)}</div><p className="mt-3 text-xs text-neutral-500">Vitrine carregada pelo próprio site, sem rastreadores ou cookies do Instagram.</p></Container></section>; }
