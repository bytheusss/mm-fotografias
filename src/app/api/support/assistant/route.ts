import { NextResponse } from "next/server";
const windows=new Map<string,{count:number;reset:number}>();
function allowed(request:Request){const key=request.headers.get("x-forwarded-for")?.split(",")[0]||"local",now=Date.now(),item=windows.get(key);if(!item||item.reset<now){windows.set(key,{count:1,reset:now+60_000});return true}if(item.count>=12)return false;item.count++;return true}
export async function POST(request:Request){if(!allowed(request))return NextResponse.json({error:"Muitas mensagens. Aguarde um minuto."},{status:429});const body=await request.json(),question=String(body.question||"").trim().slice(0,1200),history=Array.isArray(body.history)?body.history.slice(-8):[];if(!question)return NextResponse.json({error:"Escreva sua dúvida."},{status:400});const apiKey=process.env.GEMINI_API_KEY;if(!apiKey)return NextResponse.json({configured:false,answer:"Ainda não consegui consultar a assistente inteligente. Posso organizar seus dados e encaminhar tudo diretamente para a equipe no WhatsApp."});const contents=[...history.map((item:{role?:string;text?:string})=>({role:item.role==="assistant"?"model":"user",parts:[{text:String(item.text||"").slice(0,1200)}]})),{role:"user",parts:[{text:question}]}],system=`Você é a assistente virtual da M&M Fotografias, de Rio Claro/SP. Faça pré-atendimento curto, cordial e em português do Brasil. Ajude com localização de fotos, eventos, carrinho, pedidos, pagamento, download, privacidade, orçamento e contato com fotógrafos. Nunca invente status de pedido, preço ou pagamento. Quando precisar consultar dados internos, diga isso claramente e peça somente nome, WhatsApp, e-mail e número do pedido necessários. Não peça senha, cartão, código PIX nem documento. Ao final, ofereça encaminhar um resumo para o WhatsApp da equipe. Respostas com no máximo 120 palavras.`;try{const model=process.env.GEMINI_MODEL||"gemini-3.7-flash",response=await fetch(`https://generativelanguage.googleapis.com/v1beta/models/${encodeURIComponent(model)}:generateContent`,{method:"POST",headers:{"Content-Type":"application/json","x-goog-api-key":apiKey},body:JSON.stringify({systemInstruction:{parts:[{text:system}]},contents,generationConfig:{temperature:0.3,maxOutputTokens:300}}),signal:AbortSignal.timeout(15000)}),data=await response.json(),answer=data?.candidates?.[0]?.content?.parts?.map((part:{text?:string})=>part.text||"").join("").trim();if(!response.ok||!answer)throw new Error(data?.error?.message||"Gemini indisponível");return NextResponse.json({configured:true,answer})}catch(error){console.error("GEMINI SUPPORT ERROR",error);return NextResponse.json({configured:true,fallback:true,answer:"A assistente está temporariamente indisponível. Vou deixar sua solicitação pronta para continuar com a equipe pelo WhatsApp."})}}
