import { NextResponse } from "next/server";
import { supabaseAdmin } from "@/lib/supabaseAdmin";
import { getApiUser } from "@/lib/api-auth";
export async function PATCH(request: Request, { params }: { params: Promise<{ id: string }> }) { const { id } = await params; const body = await request.json(); if (body.action === "restore") { const { error } = await supabaseAdmin.from("photos").update({ deleted_at: null, deleted_by: null }).eq("id", id); return error ? NextResponse.json({ error: error.message }, { status: 500 }) : NextResponse.json({ success: true }); } const plate = typeof body.plate_text === "string" ? body.plate_text.replace(/[^a-z0-9]/gi, "").toUpperCase().slice(0, 10) : null; const { error } = await supabaseAdmin.from("photos").update({ plate_text: plate || null }).eq("id", id); return error ? NextResponse.json({ error: "Falha ao salvar placa" }, { status: 500 }) : NextResponse.json({ success: true, plate_text: plate }); }
export async function DELETE(_: Request, { params }: { params: Promise<{ id: string }> }) { const { id } = await params; const user = await getApiUser(); const { error } = await supabaseAdmin.from("photos").update({ deleted_at: new Date().toISOString(), deleted_by: user?.id || null }).eq("id", id).is("deleted_at", null); return error ? NextResponse.json({ error: error.message }, { status: 500 }) : NextResponse.json({ success: true, recoverable: true }); }
