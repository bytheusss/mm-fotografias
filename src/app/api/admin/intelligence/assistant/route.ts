import {NextResponse} from "next/server";import {isApiAdmin} from "@/lib/api-auth";import {supabaseAdmin} from "@/lib/supabaseAdmin";
export async function POST(request:Request){if(!(await isApiAdmin()))return NextResponse.json({error:"Acesso negado"},{status:403});const question=String((await request.json()).question||"").trim().slice(0,1000);if(!question)return NextResponse.json({error:"Faça uma pergunta"},{status:400});const key=process.env.GEMINI_API_KEY;if(!key)return NextResponse.json({error:"Gemini não configurado"},{status:503});const[{data:orders},{data:events},{data:tasks},{data:expenses},{data:tickets}]=await Promise.all([supabaseAdmin.from("orders").select("status,total,created_at,email").order("created_at",{ascending:false}).limit(2000),supabaseAdmin.from("events").select("name,published,archived,total_photos,event_date").limit(200),supabaseAdmin.from("event_tasks").select("title,status,priority,due_at,events(name)").limit(500),supabaseAdmin.from("event_expenses").select("amount,category,expense_date,events(name)").limit(1000),supabaseAdmin.from("support_tickets").select("status,priority,last_message_at").limit(1000)]);const context=JSON.stringify({orders,events,tasks,expenses,tickets}).slice(0,50000),model=process.env.GEMINI_MODEL||"gemini-3.7-flash";try{const r=await fetch(`https://generativelanguage.googleapis.com/v1beta/models/${encodeURIComponent(model)}:generateContent`,{method:"POST",headers:{"Content-Type":"application/json","x-goog-api-key":key},body:JSON.stringify({systemInstruction:{parts:[{text:"Você é o analista operacional interno da M&M Fotografias. Responda em português do Brasil usando exclusivamente os dados fornecidos. Não invente números. Diferencie faturamento de lucro e informe quando faltarem dados. Seja direto, com no máximo 250 palavras e sugestões práticas. Nunca execute ações."}]},contents:[{role:"user",parts:[{text:`DADOS:\n${context}\n\nPERGUNTA: ${question}`}]}],generationConfig:{maxOutputTokens:1800,thinkingConfig:{thinkingLevel:"low"}}}),signal:AbortSignal.timeout(25000)}),data=await r.json(),answer=data?.candidates?.[0]?.content?.parts?.filter((p:{thought?:boolean})=>!p.thought).map((p:{text?:string})=>p.text||"").join("").trim();if(!r.ok||!answer)throw new Error(data?.error?.message||"Resposta indisponível");return NextResponse.json({answer})}catch(error){return NextResponse.json({error:error instanceof Error?error.message:"Assistente indisponível"},{status:502})}}
