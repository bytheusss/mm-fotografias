import { NextResponse } from "next/server";
import { supabaseAdmin } from "@/lib/supabaseAdmin";
export async function GET() { const { data } = await supabaseAdmin.from("data_subject_requests").select("id,user_id,request_type,details,status,response_notes,created_at,profiles(email,full_name)").order("created_at", { ascending: false }).limit(200); return NextResponse.json({ requests: data || [] }); }
export async function PATCH(request: Request) { const body = await request.json(); const status = String(body.status || ""); if (!["open", "reviewing", "completed", "rejected"].includes(status)) return NextResponse.json({ error: "Status inválido." }, { status: 400 }); const { error } = await supabaseAdmin.from("data_subject_requests").update({ status, response_notes: String(body.notes || "").trim().slice(0, 3000) || null, updated_at: new Date().toISOString() }).eq("id", body.id); return error ? NextResponse.json({ error: error.message }, { status: 400 }) : NextResponse.json({ success: true }); }
