import type{Metadata}from"next";import{PublicPortfolio,type PortfolioItem}from"@/components/portfolio/PublicPortfolio";import{supabaseAdmin}from"@/lib/supabaseAdmin";
export const dynamic="force-dynamic";export const metadata:Metadata={title:"Portfólio | M&M Fotografias",description:"Trabalhos autorais dos fotógrafos da M&M: casamentos, retratos, eventos e fotografia automotiva."};
export default async function Page(){const{data}=await supabaseAdmin.from("photographer_portfolio_assets").select("id,storage_path,title,description,category,photographer_id,sort_order,profiles!inner(full_name,public_profile)").eq("published",true).eq("profiles.public_profile",true).order("sort_order").order("created_at",{ascending:false});const items=(data||[]).map(row=>{const profile=Array.isArray(row.profiles)?row.profiles[0]:row.profiles;return{id:row.id,url:supabaseAdmin.storage.from("portfolio-assets").getPublicUrl(row.storage_path).data.publicUrl,title:row.title||"",description:row.description||"",category:row.category||"Outros",photographer_id:row.photographer_id,photographer_name:profile?.full_name||"M&M Fotografias"}})as PortfolioItem[];return <main className="min-h-screen bg-black px-4 pb-28 pt-32 text-white sm:px-6"><div className="mx-auto max-w-7xl"><p className="font-bold uppercase tracking-[.25em] text-red-500">Olhares da nossa equipe</p><h1 className="mt-3 text-5xl font-black sm:text-7xl">Portfólio M&M</h1><p className="mb-10 mt-4 max-w-2xl text-lg text-neutral-400">Uma seleção independente dos nossos fotógrafos — além das fotos publicadas nos álbuns de eventos.</p><PublicPortfolio items={items}/></div></main>}
