import { integrationStatus } from "@/lib/integrations";

export default function IntegrationsPage() {
  const integrations = integrationStatus();
  return <main className="min-h-screen bg-black px-6 pb-20 pt-32 text-white"><div className="mx-auto max-w-4xl"><h1 className="text-4xl font-bold">Integrações</h1><p className="mb-8 mt-3 text-neutral-400">Esta tela mostra apenas o estado das variáveis da Vercel. Nenhuma chave privada é exibida.</p><div className="space-y-3">{integrations.map(item => <article key={item.name} className="flex items-center justify-between rounded-xl border border-neutral-800 bg-neutral-900 p-5"><div><h2 className="font-bold">{item.name}</h2><p className="text-sm text-neutral-400">{item.detail}</p></div><span className={`rounded-full px-3 py-1 text-sm font-bold ${item.configured ? "bg-green-900 text-green-300" : "bg-amber-950 text-amber-300"}`}>{item.configured ? "Configurada" : "Faltam credenciais"}</span></article>)}</div><div className="mt-8 rounded-xl border border-neutral-800 p-5 text-sm text-neutral-300"><p>Configure as chaves em Vercel → Project Settings → Environment Variables e faça um novo deploy. Use os mesmos nomes documentados no arquivo <code>.env.example</code>.</p></div></div></main>;
}
