import {getUser} from "@/lib/auth";import {HelpDesk} from "@/components/support/HelpDesk";export default async function Page(){const user=await getUser();return <main className="min-h-screen bg-black px-4 pb-24 pt-32 text-white sm:px-6"><div className="mx-auto max-w-6xl"><p className="font-bold uppercase tracking-widest text-red-500">Central de ajuda</p><h1 className="mt-2 text-4xl font-black sm:text-5xl">Estamos por perto.</h1><p className="mb-8 mt-3 text-neutral-400">Atendimento pelo site ou encaminhamento inteligente para o WhatsApp.</p><HelpDesk logged={Boolean(user)}/></div></main>}
