import {redirect} from "next/navigation";import {requireStaff} from "@/lib/auth";import {SupportOperations} from "@/components/support/SupportOperations";export default async function Page(){const staff=await requireStaff();if(!staff.roles.some(role=>["owner","admin","support"].includes(role)))redirect("/fotografo");return <main className="min-h-screen bg-black px-4 pb-24 pt-28 text-white sm:px-6"><div className="mx-auto max-w-7xl"><p className="font-bold uppercase tracking-widest text-red-500">V16 · Operação inteligente</p><h1 className="mt-2 text-4xl font-black">Central de atendimento</h1><p className="mb-8 mt-2 text-neutral-400">Fila, responsáveis, prioridades, conhecimento e desempenho em um só lugar.</p><SupportOperations/></div></main>}
