import webpush from "web-push";import {supabaseAdmin} from "@/lib/supabaseAdmin";
function configured(){const subject=process.env.VAPID_SUBJECT,publicKey=process.env.NEXT_PUBLIC_VAPID_PUBLIC_KEY,privateKey=process.env.VAPID_PRIVATE_KEY;if(!subject||!publicKey||!privateKey)return false;webpush.setVapidDetails(subject,publicKey,privateKey);return true}
export async function sendPush(userId:string,payload:{title:string;body?:string;href?:string|null}){if(!configured())return{sent:0,configured:false};const{data}=await supabaseAdmin.from("push_subscriptions").select("id,endpoint,p256dh,auth").eq("user_id",userId);let sent=0;for(const row of data||[]){try{await webpush.sendNotification({endpoint:row.endpoint,keys:{p256dh:row.p256dh,auth:row.auth}},JSON.stringify(payload));sent++;await supabaseAdmin.from("push_subscriptions").update({last_used_at:new Date().toISOString()}).eq("id",row.id)}catch(error){const status=(error as {statusCode?:number}).statusCode;if(status===404||status===410)await supabaseAdmin.from("push_subscriptions").delete().eq("id",row.id)}}return{sent,configured:true}}
